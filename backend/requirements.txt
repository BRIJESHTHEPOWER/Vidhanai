# Core runtime. Pinned to install cleanly on Python 3.11 (see .python-version) —
# several of these pins (pillow 10.1.0, numpy 1.24.3) have no wheels for 3.12+
# and fail to build from source there.
#
# Semantic/vector search (faiss + sentence-transformers -> PyTorch, ~2GB) is
# OPTIONAL and lives in requirements-ml.txt. Without it the app automatically
# falls back to the MongoDB keyword search.
fastapi==0.104.1
uvicorn==0.24.0
# 4.16.x is the version verified against the MongoDB Atlas cluster; the old
# 4.6.0 pin was never tested here and is prone to TLS handshake issues with
# Atlas on newer OpenSSL. certifi pins the CA bundle used for the Atlas TLS
# handshake (see app/db/connection.py).
pymongo==4.16.0
certifi
pydantic==2.5.0
python-dotenv==1.0.0
python-multipart==0.0.6
aiofiles==23.2.1
openai==1.3.5
pillow==10.1.0
numpy==1.24.3
requests==2.31.0
beautifulsoup4==4.12.2
httpx==0.25.1
groq
google-generativeai
slowapi
pyjwt
bcrypt

resend
razorpay
